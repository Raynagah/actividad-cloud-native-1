# Microservicio Base

## Endpoints disponibles
- `GET /api/status`: Devuelve el estado actual del servicio.
- `POST /api/saludo`: Genera un saludo personalizado.

## Instrucciones de Ejecución
```bash
./mvnw spring-boot:run